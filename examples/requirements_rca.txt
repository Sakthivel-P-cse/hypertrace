# Requirements for Step 3: Root Cause Analysis Engine
# Install with: pip install -r requirements_rca.txt

# Neo4j Python driver for dependency graph
neo4j==5.14.0

# Flask for web services (if integrating with incident processor)
flask==3.0.0

# For table formatting in reports
tabulate==0.9.0

# For HTTP requests (optional, for integrating with external APIs)
requests==2.31.0

# For JSON schema validation (optional)
jsonschema==4.20.0
